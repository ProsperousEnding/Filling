---
description: Filling 静态博客框架规则
globs:
alwaysApply: false
---

# Filling 仓库规则

## 仓库定位

这是一个**静态博客框架仓库**。

- 默认产品形态是 `Markdown/TOML + Git + 构建发布`
- 保留框架核心与默认站点应用层
- 不保留依赖服务端、第三方平台或运行时写入的功能

## 顶层结构

- `blog/`：默认博客内容与配置
- `src/framework/`：框架核心
- `src/site/`：默认站点应用层

## 目录职责

- `blog/content/articles/*.md`
  - 默认文章内容源

- `blog/config/*.toml`
  - 默认站点、个人资料、主题配置

- `src/framework/components/`
  - 框架级 UI 组件

- `src/framework/views/`
  - 框架内建页面，不是 demo

- `src/framework/router/`
  - 框架路由定义与路由工厂

- `src/framework/stores/`
  - 框架级状态管理

- `src/framework/adapters/markdown/`
  - 静态 Markdown 内容服务
  - 文章、分类、标签、归档、搜索统一从这里聚合

- `src/framework/config/`
  - TOML 配置加载逻辑

- `src/site/`
  - 站点入口、站点装配、少量站点壳层代码
  - 应尽量薄，不承载框架公共逻辑

## 禁止重新引入的能力

- 评论系统
- 分析统计脚本集成
- 远程 provider
- HTTP 内容源
- 运行时写 Markdown / TOML
- 在线文章编辑器
- 在线个人资料编辑器
- 站内后台
- 仅开发环境可用的管理能力

如果一个能力不能作为纯静态站点稳定运行，就不要保留。

## 代码放置规则

- 框架公共能力：放 `src/framework/`
- 站点装配代码：放 `src/site/`
- 默认内容与配置：放 `blog/`

不要再新增 `src/plugins/` 这类服务端或第三方扩展目录。

## 工程规则

修改结构后至少验证：

- `pnpm build`
- `pnpm build:lib`
